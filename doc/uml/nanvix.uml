@startuml

!include kernel.uml
!include libs.uml

namespace servers {

	namespace name {
	}

	namespace rmem {
	}
}

libs      ..>  kernel : <<use>>
servers   ..>  kernel : <<use>>
libs.name <|.. servers.name : <<implements>>
libs.rmem <|.. servers.rmem : <<implements>>

@enduml

