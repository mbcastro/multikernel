@startuml

!include kernel.uml
!include libs.uml

namespace servers {

	namespace name {
	}

	namespace rmem {
	}

	namespace semaphore {
  }
}

libs    ..>  kernel : <<use>>
servers ..>  kernel : <<use>>
libs.name      <|.. servers.name      : <<implements>>
libs.rmem      <|.. servers.rmem      : <<implements>>
libs.semaphore <|.. servers.semaphore : <<implements>>
libs.name      <..  servers.semaphore : <<use>>

@enduml
